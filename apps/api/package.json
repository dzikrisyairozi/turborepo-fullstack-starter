{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cargo watch -x run",
    "build": "cargo build --release",
    "clean": "cargo clean",
    "check": "cargo check",
    "test": "cargo test",
    "lint": "cargo clippy -- -D warnings",
    "generate-types": "cross-env TS_RS_EXPORT_DIR=../../packages/api-types/src cargo test export_bindings -- --nocapture"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
